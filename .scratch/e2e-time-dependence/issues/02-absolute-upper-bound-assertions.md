# 02 절대 ms 상한 판정 3곳이 장비 성능에 따라 거짓 실패한다

Status: done

## 현상

2026-09-24 코드 읽기로 확인(실행 없음). 벽시계 ms 상한으로 실패를 던지는 곳:

| 위치                                | 판정                  | 재는 구간                                   | 측정 방식                          |
| ----------------------------------- | --------------------- | ------------------------------------------- | ---------------------------------- |
| `checks/stdin-input-check.mjs:258`  | `elapsed > 2500` 실패 | `call_later(2, print, 'TICK')` 뒤 TICK 출현 | Node `Date.now()` + `waitFor` 폴링 |
| `checks/input-cancel-check.mjs:324` | `elapsed > 500` 실패  | Ctrl+C → `KeyboardInterrupt` 출현           | Node `Date.now()` + 25ms 폴링      |
| `checks/tab-check.mjs:637`          | `max > 200` 실패      | C12 Tab 후보 왕복(a.·빈 스템, 표본 20)      | 스크립트 내 통계                   |

하한 판정(`stdin-input-check.mjs:257` `< 1000`, `tla-check.mjs:119` `< 500`)은 느린 장비에서 더 빨라질 수 없어 해당 없음.

## 왜 문제인가

상한은 개발 장비 실측 기준이다. 느린 장비·CI에서는 제품 결함 없이 실패한다. 반대로 빠른 장비에서는 회귀가 있어도 상한 안에
들어갈 수 있다. 응답성 자체가 요구 사항(예: Ctrl+C 0.5초 내 중단)인 경우는 시간 판정을 없앨 수 없다.

Node 쪽 폴링은 추가로 과대 측정한다(`docs/traps/TRP-022`, 문턱 근처 5~8ms). `input-cancel-check.mjs`는 25ms 간격 폴링이라
오차가 더 크다.

## 제안(미검증)

- 측정을 페이지 안 `performance.now()`로 옮긴다. `measure/sleep-await-check.mjs:126-161`(`window.__ctrlCAt`·
  `__promptReadyAt`)이 선례다.
- 절대 상한 → 상대 상한: 같은 실행에서 기준 동작(예: `1+1` 왕복) 중앙값을 먼저 재고 "기준의 k배 이내"로 판정한다. 또는
  `E2E_TIME_SCALE` 배율 환경변수를 두고 기본 1.
- `stdin-input-check.mjs`의 TICK은 요구 사항이 "Enter 없이 나온다"이므로 상한을 정지 감지용(예: 10초)으로 풀고 하한만 판정으로
  남기는 안도 있다.

판정선 변경은 사용자 확인 대상이다.

## 다음에 이어받을 때

1. 세 곳 각각 요구 사항이 "응답성 수치"인지 "동작 발생 여부"인지 ROADMAP 해당 RD·`BASELINE.md`로 확인한다.
2. 응답성 수치면 상대 상한 또는 배율, 발생 여부면 상한을 정지 감지용으로 완화 — 안을 사용자에게 확인받는다.
3. 이슈 03의 CPU 감속 실행으로 수정 전 거짓 실패 재현 → 수정 후 통과를 확인한다. `ONLY=`로 해당 절만 실행한다.

## 관련

- `.scratch/e2e-baseline-drift/issues/02-sleep-await-median-over-30ms-reference.md`: `sleep-await-check.mjs` 30ms 판정선
  문제(스크립트는 단언하지 않는데 `BASELINE.md` 4절은 판정선으로 적음). 같은 뿌리지만 그 이슈에서 추적한다.

## Comments

- 2026-09-24 재분류: `deferred`(`docs/agents/issue-tracker.md` "등록·분류 기준"). 세 곳 모두 거짓 실패를 아직 관찰하지 않았다. 새 판정은 `docs/design/09-testing.md` 9.7 2항으로 흡수. 재개 조건: 세 판정 중 하나가 결함 없는 코드에서 실패하면 `open`(그 스크립트를 고칠 때 9.7로 바꾼다).
- 2026-09-26 수정 전 감속 실행(`E2E_CPU_THROTTLE=4`, dev 5173, 절별 `ONLY=`, 각 1회): **세 판정 모두 거짓 실패가 재현되지 않았다.** 아래는 있는 그대로의 값이다. 원시 로그·결과 JSON은 작업 폴더 `verify/delta03-*`.

  | 판정                                        | 명령                        | 결과     | 실측                                                                         |
  | ------------------------------------------- | --------------------------- | -------- | ---------------------------------------------------------------------------- |
  | `checks/tab-check.mjs:697` `max > 200`      | `ONLY=C12 e2e:tab`          | 4/4 통과 | a. 속성 후보 최대 30.5ms(중앙값 26.1), 빈 스템 최대 16.0ms, 미도착 0/20·0/20 |
  | `checks/input-cancel-check.mjs:324` `> 500` | `ONLY=EC e2e:input-cancel`  | 2/2 통과 | `elapsedMs` 44                                                               |
  | `checks/stdin-input-check.mjs:257`·`:258`   | `ONLY=TICK e2e:stdin-input` | 2/2 통과 | `elapsed` 1963ms(하한 1000·상한 2500 모두 안 깨짐)                           |
  - 이 감속은 메인 스레드만 늦추고 worker에는 걸리지 않는다(이슈 03 Comments: rate 4에서 메인 4.07배, worker 1.01배). 위 값은 xterm 렌더·입력 지연만 반영하고 worker 쪽 지연(`call_later` 타이머, SIGINT 처리, Python 실행)은 시험되지 않았다. 통과는 "판정선이 견고하다"는 증거가 아니라 "메인 스레드 4배 지연으로는 재현되지 않았다"는 사실이다.
  - TICK은 `started`가 프롬프트 복귀 이후라 1963ms로 타이머 2000ms보다 37ms 짧게 나왔다(하한이 줄어드는 방향은 값에 보이나 하한 1000ms까지 거리 963ms).
  - 감속 실행에서 (B) 존재 확인 부류 거짓 실패는 0곳이라 이슈 01 승격 근거는 나오지 않았다. Status는 이 작업 DELTA-05에서 바꾼다.

- 2026-09-26 완료(`done`): 세 판정을 `docs/design/09-testing.md` 9.7에 맞췄다. **규칙 적용으로 해소한 것이며 거짓 실패는 실측하지 못했다**(위 수정 전 감속 실행에서 재현되지 않았다. 확정 19에 따라 배율을 8·16으로 올려 재현을 시도하지 않았다). 이 감속은 메인 스레드만 늦추므로 수정 후 감속 통과는 판정선이 견고하다는 증거가 아니다(이슈 03 Comments).

  | 판정                                                  | 수정 내용                                                                                                                                                                                                                                                                                                 | 수정 전(감속 4, 메인만)                                                                                                  | 수정 후 감속 4 / 정상                                                                         |
  | ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------- |
  | `checks/tab-check.mjs` C12 `max > 200`                | 판정 삭제, 기록만(9.7 6항). `C12 정지 0(표본 20/20)` 판정은 유지. 중앙값·최대·미도착을 결과 JSON `notes`에 기록. 셀 수 76 → 75                                                                                                                                                                            | a. 속성 후보 최대 30.5ms(중앙값 26.1), 빈 스템 최대 16.0ms                                                               | a. 속성 후보 중앙값 26.5 / 25.2, 최대 35.4 / 35.8. 빈 스템 중앙값 9.6 / 7.0, 최대 16.3 / 16.6 |
  | `checks/input-cancel-check.mjs` EC `> 500`            | Node `Date.now()` + 25ms 폴링을 페이지 시계 `markText("KeyboardInterrupt", { exclude: "except", startOnKey: { key: "c", ctrlKey: true } })` + `readMark()`로 교체. 상한 `1000 * E2E_TIME_SCALE`ms(9.7 2항 예외: 요구 사항이 응답성 수치, `ROADMAP.md:184`. 결함 시 값은 `loop-done`이 4초 뒤 나오는 경우) | `elapsedMs` 44(Node 시계, CDP 왕복 2회/폴링 포함)                                                                        | 33.3 / 25.2(페이지 시계)                                                                      |
  | `checks/stdin-input-check.mjs` TICK `> 2500`·`< 1000` | 시작점을 프롬프트 복귀 후에서 Enter `keydown` 페이지 시각으로 이동(`markText("TICK", { exclude: "call_later", startOnKey: { key: "Enter" } })`). 하한 2000ms(`call_later(2)` 타이머 자체), 상한 판정 삭제, `readMark({ timeoutMs: 10000 })`은 정지 감지용                                                 | 1963ms(프롬프트 복귀 후 Node 시계. 타이머 2000ms보다 37ms 짧다 — 시작점이 늦어 구간이 줄어든다는 발견 ①이 값에 나타난다) | 2013.0 / 2018.0(하한 2000 대비 +13.0 / +18.0)                                                 |
  - 정상 속도 전체 실행(각 1회, dev 5173, `pageErrors` 0 · `problemLogs` 0): `e2e:tab` 75/75, `e2e:input-cancel` 26/26, `e2e:stdin-input` 19/19(`apps/demo/e2e/BASELINE.md`의 기존 셀 수와 같고 tab만 76 → 75). 감속 4 세 절(`ONLY=C12`·`EC`·`TICK`)도 통과. 원시 로그·결과 JSON은 저장소에 남기지 않았다(작업 폴더 `verify/delta03-*`·`verify/delta04-*`는 병합 뒤 `_works/_completed/`에 있다).
  - TICK 하한 여유는 Enter → `call_later` 등록 지연 몫뿐(13~18ms)이다. 하한을 깨는 환경은 이번 실행에서 재현되지 않았다.
  - EC의 `exclude: "except"`는 제출 소스 줄이 80열에서 감기는 위치(`KeyboardInterrupt`와 `except`가 첫 행에 함께 있음)에 기댄다. 소스 줄을 고치면 `exclude`를 다시 점검한다(주석에 적음). 결함 주입(양성 대조)은 하지 않았다.
  - 남은 위험: 각 1회 실행. 감속이 worker(`call_later` 타이머·SIGINT 처리)에 걸리지 않아 worker 지연에서의 거짓 실패는 시험하지 못했다. worker 대상 감속은 후속 이슈 `05-worker-cpu-throttle.md`.
